import type { Resource } from '../hooks/useAsyncResource'
import type { Weather } from '../types'
export function WeatherPanel({ resource }: { resource: Resource<Weather> }) { const { data, loading, error, reload } = resource; return <section className="weather" aria-labelledby="weather-title"><div className="section-heading"><p id="weather-title">広島市中心部の気象</p><button className="icon-button" onClick={reload} aria-label="気象情報を再取得" title="再取得">↻</button></div>{loading && <p className="muted">気象情報を取得中…</p>}{error && <p className="notice">{error} <button onClick={reload}>再試行</button></p>}{data && <><div className="weather-main"><strong>{data.temperature?.toFixed(1) ?? '—'}°</strong><span>体感 {data.apparentTemperature?.toFixed(1) ?? '—'}°C</span></div><dl className="weather-stats"><div><dt>湿度</dt><dd>{data.humidity ?? '—'}%</dd></div><div><dt>風速</dt><dd>{data.windSpeed ?? '—'} km/h</dd></div><div><dt>日射</dt><dd>{data.radiation ?? '—'} W/m²</dd></div></dl></>}</section> }
