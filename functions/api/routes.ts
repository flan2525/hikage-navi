interface Env { OPENROUTESERVICE_API_KEY?: string }
const inHiroshima = (value: string | null) => { const [lng, lat] = (value ?? '').split(',').map(Number); return Number.isFinite(lng) && Number.isFinite(lat) && lng > 132.42 && lng < 132.50 && lat > 34.36 && lat < 34.43 }
export const onRequestGet: PagesFunction<Env> = async ({ request, env }) => { const url = new URL(request.url); const start = url.searchParams.get('start'); const end = url.searchParams.get('end'); if (!inHiroshima(start) || !inHiroshima(end)) return Response.json({ error: '広島市中心部の座標のみ利用できます' }, { status: 400 }); if (!env.OPENROUTESERVICE_API_KEY) return Response.json({ error: 'ルーティングAPIが未設定です' }, { status: 503 }); const upstream = new URL('https://api.openrouteservice.org/v2/directions/foot-walking'); upstream.search = new URLSearchParams({ start: start!, end: end!, alternative_routes: 'true', 'alternative_routes.target_count': '1' }).toString(); const response = await fetch(upstream, { headers: { Authorization: env.OPENROUTESERVICE_API_KEY, Accept: 'application/geo+json' } }); return new Response(response.body, { status: response.status, headers: { 'content-type': response.headers.get('content-type') ?? 'application/json', 'cache-control': 'public, max-age=300' } }) }
